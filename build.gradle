buildscript {
    repositories {
        mavenLocal()
        maven { url = 'https://files.minecraftforge.net/maven' }
        maven { url = "https://plugins.gradle.org/m2/" }
        maven { url = 'https://repo.spongepowered.org/repository/maven-public/' }
        jcenter()
        mavenCentral()
    }
    dependencies {
        classpath group: 'net.minecraftforge.gradle', name: 'ForgeGradle', version: '5.1.+', changing: true
        classpath "com.wynprice.cursemaven:CurseMaven:2.1.1"
        classpath group: 'org.spongepowered', name: 'mixingradle', version: '0.7-SNAPSHOT'
    }
}

plugins {
    id 'eclipse'
    id 'idea'
    id 'maven-publish'
    id 'net.minecraftforge.gradle' version '[6.0,6.2)'
}

apply plugin: 'org.spongepowered.mixin'

version = mod_version
group = mod_group_id
base {
    archivesName = mod_id
}

java.toolchain.languageVersion = JavaLanguageVersion.of(17)

println "Java: ${System.getProperty 'java.version'}, JVM: ${System.getProperty 'java.vm.version'} (${System.getProperty 'java.vendor'}), Arch: ${System.getProperty 'os.arch'}"
minecraft {
    mappings channel: mapping_channel, version: mapping_version
    copyIdeResources = true

    runs {
        // applies to all the run configs below
        configureEach {
            workingDirectory project.file('run')

            // Recommended logging data for a userdev environment
            // The markers can be added/remove as needed separated by commas.
            // "SCAN": For mods scan.
            // "REGISTRIES": For firing of registry events.
            // "REGISTRYDUMP": For getting the contents of all registries.
            property 'forge.logging.markers', 'REGISTRIES'

            // Recommended logging level for the console
            // You can set various levels here.
            // Please read: https://stackoverflow.com/questions/2031163/when-to-use-the-different-log-levels
            property 'forge.logging.console.level', 'debug'

            mods {
                "${mod_id}" {
                    source sourceSets.main
                }
            }
        }

        client {
            // Comma-separated list of namespaces to load gametests from. Empty = all namespaces.
            property 'forge.enabledGameTestNamespaces', mod_id
        }

        server {
            property 'forge.enabledGameTestNamespaces', mod_id
            args '--nogui'
        }

        // This run config launches GameTestServer and runs all registered gametests, then exits.
        // By default, the server will crash when no gametests are provided.
        // The gametest system is also enabled by default for other run configs under the /test command.
        gameTestServer {
            property 'forge.enabledGameTestNamespaces', mod_id
        }

        data {
            // example of overriding the workingDirectory set in configureEach above
            workingDirectory project.file('run-data')

            // Specify the modid for data generation, where to output the resulting resource, and where to look for existing resources.
            args '--mod', mod_id, '--all', '--output', file('src/generated/resources/'), '--existing', file('src/main/resources/')
        }
    }
}

sourceSets.main.resources { srcDir 'src/generated/resources' }
repositories {
    maven {
        name = "cursemaven"
        url = "https://cursemaven.com"
        content {
            includeGroup "curse.maven"
        }
    }
    mavenCentral()
}

dependencies {
    minecraft "net.minecraftforge:forge:${minecraft_version}-${forge_version}"

    //Tetra: Requires mutil
    implementation fg.deobf("curse.maven:tetra-289712:3585032")

    //PREVIOUSLY SUPPORTED
    //Abnormals: Atmospheric, Autumnity, Bamboo Blocks, Bayou Blues, Endergetic Expansion, Environmental, Upgrade Aquatic. Require Blueprint.
    implementation fg.deobf("curse.maven:atmospheric-362393:3553332")
    implementation fg.deobf("curse.maven:autumnity-365045:3553317")
    implementation fg.deobf("curse.maven:bamboo-blocks-348215:3549078")
    implementation fg.deobf("curse.maven:bayou-blues-452344:3342861")
    implementation fg.deobf("curse.maven:endergetic-291509:3750371")
    implementation fg.deobf("curse.maven:environmental-388992:3561534")
    implementation fg.deobf("curse.maven:upgrade-aquatic-326895:3561148")

    //Atum (requires Curios)
    implementation fg.deobf("curse.maven:atum-59621:3990329")

    //Better End
    implementation fg.deobf("curse.maven:betterend-re-forked-537247:3523090")

    //BOP
    implementation fg.deobf("curse.maven:biomes-o-plenty-220318:3913914")

    //BYG
    implementation fg.deobf("curse.maven:oh-the-biomes-youll-go-247560:4024011")

    //Create (requires Flywheel)
    implementation fg.deobf("curse.maven:create-328085:3536025")

    //Enderite
    implementation fg.deobf("curse.maven:enderite-mod-for-forge-401284:3308100")

//    //Ice and Fire (requires Citadel)
//    implementation fg.deobf("curse.maven:ice-and-fire-dragons-264231:4614457")

    //Nether Agriculture
    implementation fg.deobf("curse.maven:nether-agriculture-504384:3423164")

    //Outer End
    implementation fg.deobf("curse.maven:the-outer-end-430404:3404058")

    //Quark (requires AutoRegLib)
    implementation fg.deobf("curse.maven:quark-243121:3642325")

    //Stalwart Dungeons
    implementation fg.deobf("curse.maven:stalwart-dungeons-474817:3495397")

    //Twilight Forest
    implementation fg.deobf("curse.maven:the-twilight-forest-227639:3575220")

    //Unearthed
    implementation fg.deobf("curse.maven:unearthed-406825:3636005")

    //Wyrmroost
    implementation fg.deobf("curse.maven:wyrmroost-340333:3173775")


    //SET ONE
    //Blue Skies (requires Structure Gel)
    implementation fg.deobf("curse.maven:blue-skies-312918:3411324")
    //Mo' Shiz
    implementation fg.deobf("curse.maven:mo-shiz-mod-62536:3736310")
    //Ores Above Diamonds
    implementation fg.deobf("curse.maven:ores-above-diamonds-351339:3460836")


    //SET TWO
    //Enlightend
    implementation fg.deobf("curse.maven:enlightend-502786:3693390")
//    //Voidscape
//    implementation fg.deobf("curse.maven:voidscape-251730:3342892")
    //Organics
    implementation fg.deobf("curse.maven:organics-411399:3423231")


    //SET THREE
    //End's Phantasm
    implementation fg.deobf("curse.maven:phantasm-565254:4958984")
    //Silent Gear
    implementation fg.deobf("curse.maven:silent-gear-297039:3585058")
    //Silent's Gems (requires SilentLib)
    implementation fg.deobf("curse.maven:silents-gems-220311:3353519")
    //Aquamirae (requires Obscure API)
    implementation fg.deobf("curse.maven:ob-aquamirae-536254:4420334")


    //Libraries and Dependencies
    implementation fg.deobf("curse.maven:mutil-351914:3456505") //For Tetra
    implementation fg.deobf("curse.maven:blueprint-382216:3607198") //For Abnormals
    implementation fg.deobf("curse.maven:curios-309927:4419403") //For Atum
    implementation fg.deobf("curse.maven:flywheel-486392:3535459") // For Create
    implementation fg.deobf("curse.maven:citadel-331936:3441028") // For Ice & Fire
    implementation fg.deobf("curse.maven:autoreglib-250363:3326041") //For Quark

    implementation fg.deobf("curse.maven:structure-gel-api-378802:3498310") //For Blue Skies

    implementation fg.deobf("curse.maven:silent-lib-242998:3385768") //For Silent Gear and Silent's Gems
    implementation fg.deobf("curse.maven:obscure-api-638417:4420298") //For Aquamirae



    //JEI etc
    implementation fg.deobf("curse.maven:jei-238222:5065656")
    implementation fg.deobf("curse.maven:configured-457570:4462830")
    implementation fg.deobf("curse.maven:just-enough-resources-jer-240630:4526612")


//    //Optimization
//    implementation fg.deobf("curse.maven:rubidium-574856:4494903")
//    implementation fg.deobf("curse.maven:oculus-581495:4451905")


    //Mixins
    annotationProcessor 'org.spongepowered:mixin:0.8.5:processor'

}

// This block of code expands all declared replace properties in the specified resource targets.
// A missing property will result in an error. Properties are expanded using ${} Groovy notation.
// When "copyIdeResources" is enabled, this will also run before the game launches in IDE environments.
// See https://docs.gradle.org/current/dsl/org.gradle.language.jvm.tasks.ProcessResources.html
tasks.named('processResources', ProcessResources).configure {
    var replaceProperties = [
            minecraft_version: minecraft_version, minecraft_version_range: minecraft_version_range,
            forge_version: forge_version, forge_version_range: forge_version_range,
            loader_version_range: loader_version_range,
            mod_id: mod_id, mod_name: mod_name, mod_license: mod_license, mod_version: mod_version,
            mod_authors: mod_authors, mod_description: mod_description,
    ]
    inputs.properties replaceProperties

    filesMatching(['META-INF/mods.toml', 'pack.mcmeta']) {
        expand replaceProperties + [project: project]
    }
}


// Example for how to get properties into the manifest for reading at runtime.
tasks.named('jar', Jar).configure {
    manifest {
        attributes([
                'Specification-Title'     : mod_id,
                'Specification-Vendor'    : mod_authors,
                'Specification-Version'   : '1', // We are version 1 of ourselves
                'Implementation-Title'    : project.name,
                'Implementation-Version'  : project.jar.archiveVersion,
                'Implementation-Vendor'   : mod_authors,
                'Implementation-Timestamp': new Date().format("yyyy-MM-dd'T'HH:mm:ssZ")
        ])
    }

    // This is the preferred method to reobfuscate your jar file
    finalizedBy 'reobfJar'
}

// Example configuration to allow publishing using the maven-publish plugin
publishing {
    publications {
        register('mavenJava', MavenPublication) {
            artifact jar
        }
    }
    repositories {
        maven {
            url "file://${project.projectDir}/mcmodsrepo"
        }
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8' // Use the UTF-8 charset for Java compilation
}